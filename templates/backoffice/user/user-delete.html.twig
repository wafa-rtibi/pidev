{# <form method="post" action="{{ path('app_delete_user', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this user?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Delete</button>
</form>

{# <form method="post" action="{{ path('app_delete_user', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this user?')">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-sm btn-icon btn-danger">
        <svg class="icon-20" width="20" ... </button>
</form> #} 
