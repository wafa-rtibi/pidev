{{ form_start(form, {'attr': {'class': 'form-horizontal mr-5 ml-2'}}) }}
    
    
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Montant</label>
        <div class="col-sm-10">
            {{ form_widget(form.montant, {'attr': {'class': 'form-control mr-sm-2'}}) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Donateur</label>
        <div class="col-sm-10">
            {{ form_widget(form.donateur, {'attr': {'class': 'form-control mr-sm-2'}}) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-sm-2 col-form-label">Organisation</label>
        <div class="col-sm-10">
            {{ form_widget(form.organisation, {'attr': {'class': 'form-control mr-sm-2'}}) }}
        </div>
    </div>
    <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}
