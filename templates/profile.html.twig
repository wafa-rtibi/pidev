{# templates/user/profile.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}User Profile{% endblock %}

{% block body %}
    <h1>User Profile</h1>
    <p>{{ user.username }}</p>
    <p>{{ user.email }}</p>
    {# Add more fields based on your user entity properties #}
{% endblock %}
